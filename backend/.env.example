PORT=3000

# Local development (npm run dev) — connect to Docker MySQL on mapped port 3307
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sqli_root_pass
DB_NAME=sqli_demo

# Docker Compose (used by docker compose up)
MYSQL_ROOT_PASSWORD=sqli_root_pass
MYSQL_PORT=3307
